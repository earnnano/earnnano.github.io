export default class Bip39Mnemonic {
    password: string;
    constructor(password: string);
    createWallet: (entropy: string) => {
        mnemonic: string;
        seed: string;
    };
    validateMnemonic: (mnemonic: string) => boolean;
    mnemonicToSeed: (mnemonic: string) => string;
    private randomHex;
    private calculateChecksum;
}
