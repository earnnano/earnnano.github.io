export default class AddressImporter {
    fromMnemonic(mnemonic: string, seedPassword?: string): Wallet;
    fromSeed(seed: string, from?: number, to?: number): Wallet;
    /**
     * Generates the wallet
     * @param {String} seedPassword Password for the seed
     */
    private nano;
}
export interface Wallet {
    mnemonic: string;
    seed: string;
    accounts: Account[];
}
export interface Account {
    accountIndex: number;
    privateKey: string;
    publicKey: string;
    address: string;
}
