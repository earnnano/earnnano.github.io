import { Wallet } from './address-importer';
export default class AddressGenerator {
    /**
     * Generates the wallet
     *
     * @param {String} seedPassword Password for the seed
     */
    generateWallet(entropy?: string, seedPassword?: string): Wallet;
}
